"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const filter_document_kind_1 = require("./filter-document-kind");
const graphql_1 = require("graphql");
function documentFromString(pointer, filterKinds = []) {
    let content = graphql_1.parse(pointer);
    const found = [];
    content = filter_document_kind_1.filterKind(content, filterKinds);
    if (content && content.definitions && content.definitions.length > 0) {
        found.push({
            location: 'file.graphql',
            document: content,
        });
    }
    return found;
}
exports.documentFromString = documentFromString;
//# sourceMappingURL=document-from-string.js.map